import type { Options } from '../types';
export declare function beachballIcon(seed: string | Uint8Array, { size }: Options, className?: string, style?: {
    [index: string]: string;
}): HTMLElement;
